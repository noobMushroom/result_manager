[application]
port = 8000
host = "127.0.0.1"

[database]
host = "localhost"
port = 5432
username = "postgres"
password = "password"
database_name = "users"

[redis]
host = "localhost"
port = 6379
password = "password"
database_index = 0

[message_client]
base_url= "http://127.0.0.1"
device_id= "demo_device"
api_key= "secret_api_key"
timeout_milliseconds = 10000

[jwt]
secret_token = "this is a secret token"
